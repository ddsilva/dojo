const compositionHelper = {
  add1    : ((a) => a + 1),
  min1    : ((a) => a - 1),
  div2    : ((a) => a / 2),
  add15   : ((a) => a + 15),
  id      : ((a) => a),
  addall5 : ((a, b, c, d, e) => a + b + c + d + e)
}